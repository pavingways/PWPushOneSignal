import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pavingways.app.pwpushonesignal',
  appName: 'PWPushOneSignal',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
